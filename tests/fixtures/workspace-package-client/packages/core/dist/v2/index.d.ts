// Build output, committed here on purpose: the `types` condition points at it,
// and a declaration file states shape without behaviour. Resolving to this one
// would find no request members at all.
export declare const built: true;
