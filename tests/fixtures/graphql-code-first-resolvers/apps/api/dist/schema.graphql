type Mutation {
  dispatchParcel(id: ID!): Parcel!
  recallParcel(id: ID!): Boolean!
  retireParcel(id: ID!): Boolean!
}

type Parcel {
  id: ID!
  weightGrams: Int!
}

type Query {
  health: String!
  parcel(id: ID!): Parcel
  parcels: [Parcel!]!
}
